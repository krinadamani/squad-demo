# Triage Report: PR #1842

- Generated: 2026-05-18T06:34:48.868Z
- Repo: bradygaster/squad
- Title: Fix flaky CI in CLI watch mode
- Author: octocat
- Severity: high
- Suggested owner role: lead
- LLM provider: github-copilot

## Risk flags
- High severity signal from CI/labels/content
- CI failing; merge should be blocked

## Next actions
- Confirm reproduction details and expected behavior in issue/PR thread.
- Assign primary owner and reviewer pair.
- Stabilize CI before merge; capture failing test evidence.
- Run CLI smoke tests and command routing regression checks.
- Audit flaky tests for timing and environment coupling.
- Escalate to on-call lead and require explicit human approval gate.

## Agent outputs
- Lead: **Severity: High** — CI failing on a PR touching CLI command routing (`cli-entry.ts`, watch command config) and the test harness simultaneously. The blast radius spans runtime behavior and test reliability; a broken merge would degrade the CLI for all users and mask future regressions.

- **Owner role:** Assign **Booster** (CI/CD) as primary with **FIDO** (Quality Owner) as co-reviewer. CONTROL (TypeScript) should spot-check `cli-entry.ts` routing changes for correctness. This crosses three disciplines — no solo merge.

- **Merge gate:** CI must be green before merge. Capture raw failing test output and attach to the PR thread. If the failure is non-deterministic (timing/environment), require three consecutive green runs on the integration suite before unblocking.

- **Escalation path:** Booster owns stabilization. If CI is not green within one sprint cycle, escalate to Flight (Lead) for a decision on whether to revert or quarantine the watch-mode tests behind a feature flag while the fix lands separately.

- **Approval requirement:** Requires explicit approvals from Booster + FIDO + one human (Brady or delegated lead) before merge. Do not merge on a single automated approval — the combination of flaky tests, routing changes, and high heuristic severity triggers the human gate per Squad policy.
- Developer: Building on Lead's ownership and severity framing:

**Root cause areas:** The sequential if-chain router in `cli-entry.ts` (lines 299–1003) and the watch command's 3-second `setTimeout` signal cleanup window are the most likely culprits. CI flakiness almost certainly stems from the circuit breaker's `cooldownMinutes` state not being reset between test runs, combined with process cleanup races — specifically `watch-circuit-breaker.test.ts` line 261's `setTimeout(r, 50)` being too tight under CI load. The config loader's `loadWatchConfig()` merging CLI flags over file state could also produce ordering-sensitive results if test harness setup modifies `.squad/config.json` without teardown.

**Safe fix strategy:** Isolate the fix into two atomic commits — (1) add explicit `beforeEach`/`afterEach` teardown in `watch-mode.test.ts` to reset circuit breaker state and remove any stale `.squad/config.json` fixtures; (2) increase the `setTimeout` race window in `watch-circuit-breaker.test.ts` from 50ms to at least 200ms with a code comment explaining the CI margin. Do **not** touch `cli-entry.ts` routing logic in the same PR — if routing truly needs a fix, open a follow-on PR with CONTROL's sign-off per Lead's three-discipline rule.

**Rollback plan:** If CI does not go green after the test harness fix, revert the `watch-mode.test.ts` changes and quarantine the flaky test file behind a `it.skip` with a `TODO: #1842` comment. This keeps main green without deleting evidence. The watch command itself remains fully functional at runtime — only the CI gate is affected.

**Validation bar before merge:** Three consecutive green runs on the integration suite (as Lead specified), plus a manual `squad watch --interval 1 --dispatch-mode task` smoke test in a clean temp repo to confirm `cli-entry.ts` routing was not inadvertently broken. Attach the passing run URLs to the PR thread before requesting Booster + FIDO approvals.
- Tester: Building on Developer's root cause analysis — four focused bullets:

**1. Circuit Breaker State Isolation Test**
Add a regression test in `watch-mode.test.ts` that asserts `cooldownMinutes` state reads as default/reset at the start of every `beforeEach` block. Inject a spy on the circuit breaker constructor to confirm no stale singleton carries over between test cases. Failure here directly confirms the Developer's identified inter-test contamination vector.

**2. Race Window Hardening Check**
After widening `watch-circuit-breaker.test.ts` line 261's `setTimeout` from 50ms → 200ms, add a CI job step that re-runs *only* the watch-mode suite 5× consecutively (`--repeat 5`) and fails fast on any single flake. Three-consecutive-green (Lead's gate) is not strict enough to surface 50ms-class races under CI load — 5× catches them.

**3. Config Fixture Teardown Assertion**
Write a post-suite assertion that verifies `.squad/config.json` is absent or restored to a known baseline after each test that calls `loadWatchConfig()`. This guards against the Developer-flagged ordering-sensitive merge behavior without requiring `cli-entry.ts` changes in this PR.

**4. Routing Non-Regression Smoke Test**
Run a lightweight CLI smoke test (`squad watch --help`, `squad watch --dry-run`) as a required CI step to confirm `cli-entry.ts` routing still resolves the watch command correctly after the teardown/timeout changes. Exit criterion: zero non-zero exit codes across all invocations on Linux and Windows runners. Block merge if either fails — routing correctness is a separate risk that must not regress silently while test harness changes land.
- Scribe: **PR #1842 — Triage Summary (Fix flaky CI in CLI watch mode)**

- **🚫 Blocker — Merge gated:** CI must reach 5× consecutive green on the watch-mode suite before merge is considered. Raw failing test output must be attached to this PR thread now. Do not merge on automated approval alone — explicit sign-off from Booster, FIDO, and a human lead (Brady or delegate) is required.

- **👤 Ownership:** Booster owns CI stabilization. FIDO co-reviews test harness changes. CONTROL spot-checks any `cli-entry.ts` routing lines touched. If CI is not green within one sprint cycle, Flight decides: revert or quarantine watch-mode tests behind a feature flag.

- **🔧 Fix approach — two atomic commits only:** (1) Add `beforeEach`/`afterEach` teardown in `watch-mode.test.ts` to reset circuit breaker `cooldownMinutes` state and clean up any stale `.squad/config.json` fixtures. (2) Widen the `setTimeout` race window in `watch-circuit-breaker.test.ts` line 261 from 50ms → 200ms with an explanatory comment. Routing logic in `cli-entry.ts` must not be changed in this PR — open a follow-on if needed.

- **✅ Verification plan:** Add a constructor spy asserting no stale circuit breaker singleton survives between test cases; add a post-suite assertion confirming `.squad/config.json` is absent or restored after every `loadWatchConfig()` call; run the watch-mode suite with `--repeat 5` as a dedicated CI step that fails fast on any single flake.
